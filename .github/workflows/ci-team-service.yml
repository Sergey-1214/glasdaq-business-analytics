name: CI - Team Service

on:
  push:
    paths:
      - 'services/team_service/**'
      - '.github/workflows/ci-team-service.yml'
    branches: [ main ]
  
  pull_request:
    paths:
      - 'services/team_service/**'
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
        cache-dependency-path: 'services/team_service/requirements.txt'
    
    - name: Install dependencies
      working-directory: ./services/team_service
      run: |
        pip install -r requirements.txt
        pip install pytest pytest-cov flake8 black mypy isort
    
    - name: Lint with flake8
      working-directory: ./services/team_service
      run: |
        flake8 src tests --count --statistics --max-line-length=100
    
    - name: Check formatting with black
      working-directory: ./services/team_service
      run: |
        black --check src tests
    
    - name: Type check with mypy
      working-directory: ./services/team_service
      run: |
        mypy src
    
    - name: Run tests
      working-directory: ./services/team_service
      continue-on-error: true 
      run: |
        if [ -d tests ] && [ "$(ls -A tests/ 2>/dev/null)" ]; then
          pytest tests/ -v --cov=src --cov-report=xml --cov-report=term-missing
        else
          echo "⚠️ No tests yet - skipping"
        fi
        
    - name: Build Docker image
      if: github.ref == 'refs/heads/main'
      working-directory: ./services/team_service
      run: |
        docker build -t team-service:latest .
    
#    - name: Test Docker image
#      if: github.ref == 'refs/heads/main'
#      run: |
#        docker run -d --name test-team -p 8003:8003 team-service:latest
#        sleep 5
#        curl -f http://localhost:8003 || exit 1
#        docker stop test-team