name: CI - Product Service  

on:
  push:
    paths:
      - 'product_service/**'  
      - '.github/workflows/ci-product.yml'
    branches: [ main ]
  
  pull_request:
    paths:
      - 'product_service/**'  
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
        cache-dependency-path: 'product_service/requirements.txt'
    
    - name: Install dependencies
      working-directory: ./product_service
      run: |
        pip install -r requirements.txt
        pip install pytest pytest-cov flake8 black mypy isort
    
    - name: Lint with flake8
      working-directory: ./product_service
      continue-on-error: true
      run: |
        flake8 src tests --count --statistics --max-line-length=100
    
    - name: Check formatting with black
      working-directory: ./product_service
      continue-on-error: true
      run: |
        black --check src tests
    
    - name: Type check with mypy
      working-directory: ./product_service
      continue-on-error: true
      run: |
        mypy src
    
    - name: Run tests
      working-directory: ./product_service
      continue-on-error: true 
      run: |
        if [ -d tests ] && [ "$(ls -A tests/ 2>/dev/null)" ]; then
          pytest tests/ -v --cov=src --cov-report=xml --cov-report=term-missing
        else
          echo "⚠️ No tests yet - skipping"
        fi
    
    - name: Build Docker image
      if: github.ref == 'refs/heads/main'
      working-directory: ./product_service
      run: |
        docker build -t product-service:latest .
    
#    - name: Test Docker image
#      if: github.ref == 'refs/heads/main'
#      run: |
#        docker run -d --name test-product -p 8002:8002 product-service:latest  
#        sleep 5
#        curl -f http://localhost:8002 || exit 1  
#        docker stop test-product
